# uMap errors and their significations

## Cannot determine latitude and longitude columns. {: #error-latlon-columns }

It happens if you provide a CSV file without column names explicitly starting with `lat` and `lon`.
